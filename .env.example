#backup database information
# Database connection settings
# Make sure to replace these with your actual database credentials
# Supported types: mysql, postgresql, sqlite, etc.
# For PostgreSQL, you might use: pgsql, postgres, or postgresql
# For MySQL, you might use: mysql, mariadb, or mysql2
# For SQLite, you might use: sqlite or better-sqlite3
# For MongoDB, you might use: mongodb or mongoose
# Example for PostgreSQL:
DB_CONNECTION=pgsql # or mysql, sqlite, etc.
DB_HOST=localhost
DB_PORT=5432
DB_NAME=your_database_name
DB_USER=your_database_user
DB_PASSWORD=your_database_password

# primsa settings ex DB_TYPE = postgres, mysql, sqlite, etc.
DATABASE_URL="DB_TYPE://DB_USER:DB_PASSWORD@localhost:5432/DB_NAME?schema=public"
# Backup directory
BACKUP_DIR=./storage